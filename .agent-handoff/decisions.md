# Decision Log

| Date | Decision | Reason | Evidence |
| --- | --- | --- | --- |
| 2026-07-10 | 五个 IsaacLab uv sources 使用 portable sibling paths `../IsaacLab/source/...` editable 安装；另一台电脑把两个仓库放在同一父目录，并将 IsaacLab checkout 到 `b4c321024792976150ca55fddb26fa34480d974e`。不使用用户目录绝对路径，也不使用 Git monorepo subdirectory wheel。 | 相对 sibling path 消除 `/home/am345` 耦合，同时保留 IsaacLab `source/<distribution>/config/extension.toml` 与 Python package 的运行时相邻目录合同。 | `uv lock` 与 `uv sync --locked` 通过，lock 只记录 `../IsaacLab/...`；AppLauncher registry probe 返回 `SerialLeg-Flat-ClosedChain-v0`。对照试验中固定 Git SHA 的 subdirectory wheels 可构建安装，但导入 `isaaclab.app` 因 site-packages 缺少相邻 `config/extension.toml` 失败。 |
| 2026-07-10 | 基础迁移验收阶段只使用 IsaacLab 官方 manager-based locomotion rewards；旧 flat 自定义奖励适配推迟到 finetune。跳跃不进入当前范围，`velocity_height` 的 jump command 默认关闭且 3 个 jump slots 保持零。 | 先用最小、官方维护的奖励集合隔离并验证 SerialLeg 从原后端迁移到 Isaac Sim 后的物理、观测、动作和训练链路问题，避免同时引入旧奖励复杂语义；跳跃会额外引入阶段机、轨迹和接触 gating，不利于基础验收。 | 用户明确要求先只用官方基础 locomotion 奖励，确认迁移无大问题后再于 finetune 适配奖励，并要求当前不做跳跃、只写文档不改代码。 |
| 2026-07-10 | 暂不修改 legacy command observation scale `(2.0, 0.25, 5.0, 5.0, 5.0)`；中心化/按最终范围归一化列为低优先级，推迟到 finetune 阶段。 | 当前优先完成行为迁移；改 scale 会改变 policy 输入分布并影响旧 checkpoint、play、deploy 和 sim2sim，一次性处理更安全。 | 用户明确要求先搁置、写入交接文档并在后续 finetune 再做；旧 flat 最终 `vx` range 可达 `±2.4 m/s`，现 scale 下 actor 输入为 `±4.8`，因此保留后续优化项。 |
| 2026-07-10 | viewer 不为四根 continuous active rods 虚构独立 joint limit；每根提供一个 `2π` 操作窗口，并以 fixed-tendon linear coordinate 对 changed rod target 做联合限位投影。 | 真实模型允许两杆共同整周旋转，机械限制施加在两杆夹角而非各自绝对角；旧 standing `±0.35 rad` slider 错误收窄了单杆运动。 | contract 派生左右 coupled range `[0,1.50953527005] rad`；headless demo 与 GUI creation gates 通过。 |
| 2026-07-10 | 将方案 B 正式落为当前 runtime：左右 geometry-free virtual revolute/mount 作为零 gearing/force-coefficient PhysX fixed-tendon roots；两个 roots 保留为窄限位真实 DOF但不配置 actuator。 | 原 sibling axes 没有合法共同 tendon root；该拓扑保持每侧共同祖先，同时用窄限位和质量/惯量补偿把新增自由度影响限制到可测范围。 | USD gate 为 13/12/2 tendons；CPU 64-step root drift `8.453e-07 rad`、coupled coordinate 约 `1.316677 rad`、loop residual `3.111e-07 m`；Gym task CPU gate 通过。 |
| 2026-07-10 | external-loop A/B 的默认 separation ratio 从 `1000x` 调整为 `400x`，保留 closed `<2e-5 m`、open `>1e-3 m` 等绝对门槛。 | 双 mount/fixed-tendon 拓扑改变了同一外力 fixture 的右侧 open-control 响应；绝对 residual 仍有两个数量级以上分离，旧倍率门槛不再反映当前结构。 | 240-step CPU A/B：left `1895.9x`；right closed/open=`1.018e-05/4.196e-03 m`=`412.2x`。 |
| 2026-07-10 | fixed-tendon 路线先采用 URDF-only 方案 B 原型：左右各插入一个 narrow-range virtual revolute/mount，用户检查前不转 USD。 | 原两根 active rods 是 floating `base_link` 下的 sibling joints，缺少合法 fixed-tendon root axis；每侧独立 virtual root 可形成清晰的共同祖先，同时把潜在额外自由度限制在单侧。用户明确选择方案 B 并要求先检查 URDF。 | candidate URDF 静态 gate 为 13 links/12 joints；四个 source roots 正确 reparent；virtual range 严格非 locked；zero-pose aggregate mass/COM/inertia 守恒，FK/loop residual 不变。PhysX 零 gearing/runtime 行为仍为 `UNKNOWN`，未提前转 USD。 |
| 2026-07-10 | SerialLeg task 固定 `dt=0.005`、`decimation=4`、articulation solver `16/4`；task contact+active-control pose residual gate 为 `1e-3 m`，不替代 asset free/contact 的 `1e-5/2e-4 m` gates。 | 用户明确指定 dt/decimation。Kyber IsaacLab 未设 attachment residual gate，高冲击闭链用 `8/4`；Kyber MuJoCo pose projection 在 `>1e-3` 警告。SerialLeg `8/4 @ .005/4` 稍超 1e-3，只加倍 position iterations 后 CPU/CUDA 稳定通过；避免采用过高 `48/12`。 | 最终 CPU/CUDA compact zero residual `3.956e-4/4.756e-4 m`，controlled `5.298e-5/5.399e-5 m`；状态有限、接触非零、passive effort 为 0。 |
| 2026-07-10 | CUDA task smoke 保留默认 capacity 路径，另提供显式 `--compact-gpu-buffers` 作为单环境低显存 gate；不把 compact 值写入训练 env 默认。 | 默认 2**23 contact buffer 在 GPU 被其他训练占用 5.3 GiB 时额外请求 640 MiB 失败；compact 值足够 1 env，但不能推广到未来 4096 env。 | 同一 SerialLeg CUDA task 默认 capacity 在 rollout 前 OOM，compact capacity 完整跑过 reset + 128 env steps 并通过所有物理 gate。 |
| 2026-07-10 | SerialLeg 人工配置改为 Kyber 风格 `robot_config.yaml`，但不复制 Kyber 的位置参数数组；关节按语义 group + 具名 profile 组织，topology/dynamics/loop frames 从 canonical URDF 派生。 | YAML 更适合阅读与注释；移除与 URDF 重复的 links/edges/frames 可减少漂移，具名 profile 比 Kyber 8 列 positional row 更难错位。强类型 loader 与 artifact SHA gate 继续保留可审计性。 | YAML loader probe 派生 11/10/2 与 6 policy/4 passive；MJCF→URDF check、USD rebuild/`--check`、CPU free-space 和 closed-chain A/B 均通过；direct wheel 包含 YAML 和新 USD。 |
| 2026-07-10 | SerialLeg IsaacLab runtime 直接通过 contract-driven `UsdFileCfg` 加载已验收 USD；不再启用 MJCF importer、不再用 custom spawner 重建 loop joints。 | USD 已包含 importer-generated tree articulation 和 authored excluded external spherical constraints；runtime 重建会重复约束并重新引入旧 local-frame/prim-hierarchy 错误。 | `serialleg.py` 配置导入输出 `spawn=UsdFileCfg` 且 USD `exists=True`；旧 spawner/helper 搜索为 0；独立 USD CPU/CUDA asset gates 已先行通过。 |
| 2026-07-10 | 将主资产文件 basename、MJCF model、URDF robot、contract robot_name、converter gates 和 USD default prim 全部统一为 `serialleg_closed_chain_complex_collision`；第二个 MJCF 使用 `_isaaclab_import` 用途后缀。 | 用户明确要求所有相关代码引用也同步更名；完整迁移可避免文件名与内部合同继续分裂。两个 MJCF 不能共用同一 `.xml` 路径，因此兼容版必须保留用途后缀。 | 业务源码旧名称为 0；新 default prim `/serialleg_closed_chain_complex_collision`；URDF/USD 重建、CPU asset/closed-chain 和 viewer `both` gates 通过。 |
| 2026-07-10 | 将本地仓库历史重写为当前 collision-only 状态的单一 root commit，并删除旧 remote-tracking/Codex refs 后 aggressive GC；远端 force-push 单独等待授权。 | 旧 230 MiB mesh 已进入初始提交，普通删除提交无法缩小 `.git`；新根快照能彻底移除旧 blobs，同时 bundle 提供本地恢复。force-push 会改写 GitHub 公共历史，需独立授权和 lease 保护。 | 新 root `72a2cd8`；`.git` `70987926 → 1110734 bytes`；pack objects `461.35 KiB`；unreachable=0；bundle `61899823 bytes`；remote main 仍为 `87e145a`。 |
| 2026-07-09 | 使用多文档 Agent 接力记忆。 | 保持当前状态易读，同时把决策、验证、待办和风险保存在专用文件中。 | Bootstrap action. |
| 2026-07-09 | 以 IsaacLab 官方 external / manager-based / `rsl_rl` 模板作为新仓库起点。 | 用户要新开仓库迁移 flat 训练，先要官方模板骨架。 | `/home/am345/IsaacLab/isaaclab.sh --new` 生成 `/home/am345/se3_rl_lab`；task id 为 `Template-Se3-Rl-Lab-v0`。 |
| 2026-07-10 | handoff 记忆文件改为随仓库版本控制，不再由 `.gitignore` 排除；跟踪 `AGENT_HANDOFF.md`、`AGENT_SESSION_PROMPTS.md` 与 `.agent-handoff/*.md`。 | 用户需要在另一台电脑继续工作，Git 同步比本机私有状态更可靠；发布前敏感信息扫描未发现 token、私钥或 API key。 | 用户明确要求取消 handoff gitignore、提交推送并合并 PR；原 2026-07-09 本地私有决定被本条取代。 |
| 2026-07-09 | 本轮先沿用当前完整 Isaac Sim lock 方案，把真实 `uv sync` 跑通。 | 用户明确要求“继续迁移，下一步做跑通uv sync这一个”；先验证现有方案可落地，再决定是否简化。 | `uv sync` passed，创建 `.venv` 并安装 247 packages。 |
| 2026-07-09 | 将“SerialLeg closed-chain MJCF 直接导入并在同一 reduced articulation 内闭环”的路线判定为受阻；此结论不再外推为 PhysX 所有真闭链表达都不可行。 | IsaacSim MJCF importer 对当前 XML 的 site-based equality 和 tendons 分别失败；手工补 spherical joints 时未把闭合边排除出 articulation，PhysX 因而将 cycle 识别为 unsupported closed articulation。 | `/tmp/serialleg_closedchain_smoke.log` 含 `closed articulation, which is not supported`；direct importer 变体测试显示无 equality/tendon 才能导入；`serialleg.py` 当前 loop joints 未 authored `excludeFromArticulation`。 |
| 2026-07-09 | 暂时不让 `*.obj` mesh 走 Git LFS，改为普通 Git 文件提交。 | GitHub 远端账号/仓库 LFS budget exceeded，导致首个 push 被拒；当前 OBJ 单文件低于 GitHub 100MB 硬限制，普通 Git 可完成推送。 | 首次 `git push -u origin main` 失败并返回 LFS budget exceeded；移除 `.gitattributes` 的 `*.obj` LFS 规则、amend 到 `87e145a` 后 push 成功。 |
| 2026-07-10 | 下一阶段以 Kyber 风格真闭链资产管线为主：`closed-chain MJCF → 开链树形 URDF + loop_joint → Isaac URDF importer → 树形 Articulation + excluded external spherical constraints USD`；暂不修改实现。 | Kyber 的 K1/Lens/K2 实际 USD 证明 loop closure 可以作为 authored `physics:excludeFromArticulation=True` 的普通 PhysX spherical joints 存在，同时树内 DOF 仍由标准 IsaacLab `ArticulationCfg` 控制。该表达与 NVIDIA 对 articulation closed loop 的官方语义一致，也直接针对当前 SerialLeg 手工 loop joints 缺少 exclude flag 的问题。 | `/home/am345/bioinnov_ws/kyber_rl_lab/kyber_assets/{K1,lens,K2-v0}/*.urdf|*.usd`、`kyber_assets/convert_usd.py`、`source/.../assets/kyber.py` 的只读核对；用户明确确认“先拆成开链 URDF，再配合外部闭环约束”，并要求当前只记录方案。 |
| 2026-07-10 | 以 canonical MJCF 为唯一源，通过仓库内确定性脚本生成 SerialLeg 开链 URDF；10 个树内 hinge 输出为 `continuous`，两条 equality closure 输出为 spherical `loop_joint`。不把 base 初始 `z=0.22` 烘焙进 root，也不在标准 URDF 中伪造 fixed tendon limits、armature、contact/solver、spatial tendon 或 keyframe 语义。 | 本仓库和旧仓库均没有可复用 SerialLeg URDF；自动转换和强不变量校验比手工维护 77 个 mesh transform 更可审计。MJCF joint limits 由 fixed tendon 耦合给出，给单个 URDF joint 发明独立 limit 会改变机构语义。 | `scripts/convert_serialleg_mjcf_to_urdf.py`、生成的 `assets/robots/serialleg/urdf/serialleg_closed_chain_v3_train_obb_trim.urdf`；`--check`、独立惯量/几何审计和 Isaac Sim 5.1 `URDFParseFile` 均通过。 |
| 2026-07-10 | 使用 Isaac Sim 5.1 URDF importer 2.4.30 生成自包含 USD；floating tree articulation 保留 10 个 revolute DOF，两条 spherical loop 明确 authored `excludeFromArticulation=True`。树内恢复 source armature/viscous damping，但 drive 全部保持中性；loop 按 Kyber precedent 使用 joint-level armature `0.005`。 | 这使 reduced-coordinate articulation 保持无环，同时闭合边由普通 PhysX constraints 求解；中性 drive 避免把训练控制器固化进资产。`replace_cylinders_with_capsules=False` 保持 wheel 接触几何。 | `scripts/convert_serialleg_urdf_to_usd.py --check` 报告 11 links/10 tree joints/2 external loops；`scripts/smoke_serialleg_usd.py` CPU 64-step 报告 10 DOFs、最大 residual `3.388e-07 m`，无 `closed articulation`。 |
| 2026-07-10 | canonical/import MJCF、canonical URDF 和 runtime USD 全部采用 collision-only；源链删除 23 个高模 visual，import 时仅临时添加 11 个透明 `1e-6 m` sphere 并在保存前移除。淘汰旧 surrogate/fidelity MJCF，只保留 canonical 闭包精确需要的 54 个 collision STL。 | 用户已用 collision preview 完成视觉验收且明确 visual 不重要；让源链也 collision-only 可删除约 230 MiB 高模/旧分解，同时保持确定性重建和所有物理 gate。 | MJCF→URDF `--check` 为 0 visual/54 collision meshes；USD `--check` 为 54 meshes/7102 faces/2 cylinders/0 visual；CPU/CUDA contact、closed-chain A/B、viewer demo 均保持原数值；direct wheel `309906 bytes`。 |
| 2026-07-10 | `534237-byte` collision-only USD 通过精确 `.gitignore` / `.gitattributes` 例外作为普通 Git 资产交付，不再采用 ignored generate-then-build 边界。 | 文件已低于普通 Git 100MB 限制且无需占用已耗尽的 LFS quota；随仓库交付可让 runtime 切换和 wheel 构建不依赖本机先启动 importer。 | `git check-attr` 显示 filter/diff/merge/text 均 unset；USD SHA256 `1f60ac...29523`；direct wheel 包含该 `534237-byte` USD。 |
| 2026-07-10 | collision-only USD 在 flatten 后将 mesh collision schema 规范化到实际 `Mesh` leaves，而非保留 URDF importer 的 `Xform` wrapper。 | Physics collider overlay 对 direct primitive 可见、对本资产的 wrapper mesh 不可靠；直接 mesh schema 与 IsaacLab 标准 mesh collision 写法一致，并可由静态 gate 精确验收。 | 重建后 `direct_mesh_colliders=54`、`wrapper_colliders=0`、全部 `convexHull`；USD `--check`、64-step CPU smoke、viewer headless gate 通过。 |
| 2026-07-10 | 使用 `serialleg_contract.toml` 作为 SerialLeg converter 与 IsaacLab asset/task 的单一参数来源；USD metadata 锁 contract SHA。 | 参考 Kyber 的 config-driven converter，避免 topology、policy/passive 分组、armature/dynamics、actuator/action scale 和 importer 设置在多个 Python 文件漂移，同时保留本项目更严格的静态/runtime gates。 | contract loader 11/10/2 与 6-policy/4-passive probe 通过；asset 参数逐值一致；USD rebuild/`--check`/64-step smoke 和 wheel 包含性通过。 |
| 2026-07-10 | collision viewer 的默认实体预览必须覆盖 runtime USD 的全部 collision geometry schema；当前强制为 54 `Mesh` + 2 wheel `Cylinder`。 | 只复制 `Mesh` 会让物理 USD 中仍存在的 wheel cylinder 在 `render` 模式消失，造成误导性的 collision 目视验收。 | 5-frame CPU/headless viewer 报告 `preview_meshes=54 preview_cylinders=2`；圆柱副本保留 radius/height/axis/extent 与 world transform。 |
| 2026-07-10 | 将 USD asset smoke 分为 free-space 和 ground-contact 两个默认 gate：前者 64 步/`1e-5 m`，后者 400 步/`2e-4 m`，并要求 custom wheel Cylinder 与 mesh-backed body 均有 `>=1 N` contact report。 | 落地与无控制后续 mesh 冲击的 CPU/CUDA residual 峰值为 `9.380e-05/1.175e-04 m`，不应放宽已稳定通过的 free-space gate，也不应用 CPU-only 阈值误拒 CUDA 路径。 | CPU/CUDA default ground commands 均退出 0；wheel contact tensors 分别在 `cpu` / `cuda:0`；free-space 回归仍为 `3.388e-07 m`。 |
| 2026-07-10 | task runtime 切换必须等待独立 dynamic closed-chain-effect 验收通过；不以静态 schema、初始 pose 对齐、free-space 短 smoke 或地面 contact force 单独代替闭链效果证据。 | 用户明确纠正顺序为“下一步是检查闭链效果，还没有到 task runtime 阶段”；动态激励与 constraints on/off A/B 才能证明 external spherical joints 在主动维持 closure。 | CPU/CUDA A/B 均通过；closed joint motion 约 `0.806 rad`，closed peaks `<=5.765e-06 m`，disabled control `0.08859–0.2584 m`，最低差异 `15366.8x`。 |
| 2026-07-10 | collision viewer 默认使用 viewer-only fixed base，仅命令 6 policy joints（4 leg position offsets + 2 wheel velocities），4 passive joints 保持无直接命令；preview geometry 必须挂在运动刚体下以跟随闭链动作。 | 静态 world-space preview 在关节旋转后会留在原地，无法目视验收 closure；固定 base 可避免反作用导致机器人离开镜头，同时 `--floating-base` 保留可选路径。 | render demo 240-frame 与 both demo 30-frame 通过；GUI 控制面板成功创建；max residual `1.122e-04 m < 2e-04 m`，active/passive 都有非平凡运动。 |

## Decision Guidelines

- 只记录会长期影响项目的决策。
- 写清原因和证据。
- 不把猜测记录成决策。
- 缺少证据时标为 `UNKNOWN`。
